<?xml version="1.0" encoding="utf-8" ?> 
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:DigiReceipt"
             x:Class="DigiReceipt.Login"
             Style="{StaticResource PageStyle}"
             Title="{local:Translate txtLoginPageTitle}">
    
    <ContentPage.Content>
        <StackLayout HorizontalOptions="CenterAndExpand">
            <Label Text="{local:Translate txtLoginTitle}" HorizontalOptions="Center" FontSize="Small" FontAttributes="Bold"/>
            <Button Text="{local:Translate txtLoginWithGoogleButton}" Style="{StaticResource LoginButtonStyle}" BackgroundColor="#DE4A31" Image="Icons/google.png" Clicked="OnLoginWithGoogle"/>
            <Button Text="{local:Translate txtLoginWithMicrosoftButton}" Style="{StaticResource LoginButtonStyle}" BackgroundColor="#00ADEF" Image="Icons/microsoft.png" Clicked="OnLoginWithMicrosoft"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>